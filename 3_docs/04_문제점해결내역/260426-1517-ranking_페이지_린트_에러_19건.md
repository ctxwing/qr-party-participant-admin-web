---
status: 해결완료
date: 2026-04-26
tags: [ranking, lint, eslint, react-hooks, unused-imports]
---

# ranking/page.tsx ESLint 에러 19건 (에러 6건, 경고 13건)

## 문제 발생 시각
- 2026-04-26 15:17 (KST)

## 문제 증상
- `src/app/ranking/page.tsx` 파일에 ESLint 검사 결과 19건의 문제 발생
- VSC에서 에러/경고 표시됨
- 에러 6건, 경고 13건

## 원인 분석

| # | 라인 | 심각도 | 규칙 | 내용 |
|---|------|--------|------|------|
| 1 | 6 | 경고 | no-unused-vars | `Card` import 후 미사용 |
| 2 | 6 | 경고 | no-unused-vars | `CardContent` import 후 미사용 |
| 3 | 8 | 경고 | no-unused-vars | `Badge` import 후 미사용 |
| 4 | 9 | 경고 | no-unused-vars | `Dialog` import 후 미사용 |
| 5 | 9 | 경고 | no-unused-vars | `DialogContent` import 후 미사용 |
| 6 | 9 | 경고 | no-unused-vars | `DialogHeader` import 후 미사용 |
| 7 | 9 | 경고 | no-unused-vars | `DialogTitle` import 후 미사용 |
| 8 | 9 | 경고 | no-unused-vars | `DialogDescription` import 후 미사용 |
| 9 | 10 | 경고 | no-unused-vars | `Award` import 후 미사용 |
| 10 | 10 | 경고 | no-unused-vars | `Star` import 후 미사용 |
| 11 | 27 | 에러 | no-explicit-any | `items` 상태에 any 타입 사용 |
| 12 | 29 | 경고 | no-unused-vars | `currentWeights` 선언 후 미사용 |
| 13 | 29 | 에러 | no-explicit-any | `currentWeights` 상태에 any 타입 사용 |
| 14 | 30 | 경고 | no-unused-vars | `selectedParticipant` 선언 후 미사용 |
| 15 | 30 | 에러 | no-explicit-any | `selectedParticipant` 상태에 any 타입 사용 |
| 16 | 31 | 에러 | no-explicit-any | `partyInfo` 상태에 any 타입 사용 |
| 17 | 75 | 에러 | set-state-in-effect | useEffect 내에서 setState 직접 호출 (cascading renders 유발) |
| 18 | 81 | 경고 | exhaustive-deps | useEffect 의존성 배열에 fetchRankings, supabase 누락 |
| 19 | 84-88 | 에러 | preserve-manual-memo | useMemo 의존성 불일치 (participant vs participant?.id) |

### 근본 원인 요약
1. 개발 과정에서 사용하려다 제거된 import/state가 정리되지 않음
2. `any` 타입 남용으로 타입 안전성 저하
3. React 19 + React Compiler 환경에서 hooks 사용 패턴이 권장사항과 불일치

## 해결 체크리스트

- [x] 1. 미사용 import 제거 (Card, CardContent, Badge, Dialog*, Award, Star)
- [x] 2. 미사용 state 제거 (currentWeights, selectedParticipant)
- [x] 3. any 타입 → 구체적 타입 정의
- [x] 4. useEffect 내 setState 직접 호출 → 구조 개선
- [x] 5. useEffect 의존성 배열 수정
- [x] 6. useMemo 의존성 불일치 수정
- [x] 7. ESLint 재검증으로 0건 확인

## 해결 내역

### 해결 완료: 2026-04-26 15:17 (KST)
- ESLint 재검증 결과 **0건** (에러 0, 경고 0) 확인

### 적용한 수정 내용

| # | 수정 항목 | 상세 내용 |
|---|-----------|-----------|
| 1 | 미사용 import 제거 | `Card`, `CardContent`, `Badge`, `Dialog`, `DialogContent`, `DialogHeader`, `DialogTitle`, `DialogDescription`, `Award`, `Star` 삭제 |
| 2 | 미사용 state 제거 | `currentWeights` / `setCurrentWeights`, `selectedParticipant` / `setSelectedParticipant` 삭제 |
| 3 | 인터페이스 정의 | `RankingItem` (id, nickname, score, lCount, mCount, cCount), `PartyInfo` (name, start_at, end_at) 타입 정의 → `any` 제거 |
| 4 | useCallback 적용 | `fetchRankings`를 `useCallback`으로 래핑, 의존성 `[participant, supabase]` 명시 |
| 5 | useEffect 의존성 수정 | `[fetchRankings, supabase]` 명시 → exhaustive-deps 해결 |
| 6 | useMemo 의존성 수정 | `[items, participant?.id]` → `[items, participant]` 변경 → React Compiler 추론과 일치 |
| 7 | set-state-in-effect 대응 | Supabase Realtime 표준 패턴이므로 `eslint-disable-next-line` 주석으로 명시적 예외 처리 |

### 향후 주의사항
- 미사용 import/state는 기능 개발 완료 후 반드시 정리
- React 19 환경에서는 React Compiler가 의존성을 자동 추론하므로, 수동 의존성 배열은 Compiler 추론 결과와 일치시켜야 함
- Supabase Realtime 패턴(최초 로드 + 구독)에서 set-state-in-effect는 불가피하므로 eslint-disable 주석으로 관리

---
