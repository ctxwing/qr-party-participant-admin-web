# 다음을 수행한다.
- openspec-apply init-mvp 를 수행한다
- 코드의 주석, 답변 등은 반드시 한글로 한다. 

## Tasks.md 관리 규칙
1. 개발의 phase 또는 큰 task (1, 1.1 등)가 완료되면:
   - `3_prj_docs/완료내역/TASK_<X>_완료내역.md` 파일 작성
   - 파일 내부에 작성일자 기록
2. 각 타스크 완료 시 `/specs` 이하의 해당 `tasks.md`에 완료 체크
3. git commit 및 git push 실행
   - 커밋 메시지는 **반드시 한글**로 기록
   - 개행 문자 등을 사용하여 한 줄로 표시